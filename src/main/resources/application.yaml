spring:
  application:
    name: coaching
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:postgresql://localhost:5433/coaching_saas
    username: postgres
    password: patra
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss}){faint} %clr(%-5level){green} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %msg%n%wEx"

server:
  port: 8085

jwt:
  secret: coaching-saas-jwt-secret-key-change-this-in-production-2026
  expiration: 86400000
